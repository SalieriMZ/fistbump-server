diff --git a/src/platform/app/sdl/sdl_app.c b/src/platform/app/sdl/sdl_app.c
index 81904a3b..3587f51b 100644
--- a/src/platform/app/sdl/sdl_app.c
+++ b/src/platform/app/sdl/sdl_app.c
@@ -123,6 +123,8 @@ static int pre_init() {
     SDL_SetAppMetadata(app_name, "0.1", NULL);
     SDL_SetHint(SDL_HINT_VIDEO_WAYLAND_PREFER_LIBDECOR, "1");
     SDL_SetHint(SDL_HINT_NO_SIGNAL_HANDLERS, "1");
+    // High-precision timer for accurate frame pacing during netplay
+    SDL_SetHint(SDL_HINT_TIMER_RESOLUTION, "1");
 
     if (!SDL_Init(SDL_INIT_VIDEO)) {
         SDL_Log("Couldn't initialize SDL: %s", SDL_GetError());
