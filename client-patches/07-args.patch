diff --git a/src/args.c b/src/args.c
index 6869802f..a0f3f7a5 100644
--- a/src/args.c
+++ b/src/args.c
@@ -59,6 +59,12 @@ void init_args(int argc, const char* argv[]) {
         OPT_STRING(0, "p2p-remote-ip", &args.netplay.p2p_remote_ip, "Remote player IP.", NULL, 0, 0),
         OPT_STRING(0, "matchmaking-ip", &args.netplay.matchmaking_ip, "Matchmaking server IP.", NULL, 0, 0),
         OPT_INTEGER(0, "matchmaking-port", &args.netplay.matchmaking_port, "Matchmaking server port.", NULL, 0, 0),
+        OPT_STRING(0, "username", &args.netplay.username, "Account username for login/register.", NULL, 0, 0),
+        OPT_STRING(0, "password", &args.netplay.password, "Account password for login/register.", NULL, 0, 0),
+        OPT_BOOLEAN(0, "register", &args.netplay.do_register, "Register a new account instead of login.", NULL, 0, 0),
+        OPT_STRING(0, "room-code", &args.netplay.room_code, "Join private room with this code after login.", NULL, 0, 0),
+        OPT_STRING(0, "token", &args.netplay.token, "JWT refresh token (passed from launcher).", NULL, 0, 0),
+        OPT_STRING(0, "queue", &args.netplay.queue_mode, "Queue type: casual or ranked (default casual).", NULL, 0, 0),
 #endif
 
 #if STATCHECK
diff --git a/src/args.h b/src/args.h
index a00baba7..183e01c3 100644
--- a/src/args.h
+++ b/src/args.h
@@ -7,6 +7,12 @@ typedef struct NetplayArgs {
     const char* p2p_remote_ip;
     const char* matchmaking_ip;
     int matchmaking_port;
+    const char* username;
+    const char* password;
+    int do_register; // 1 = REGISTER instead of LOGIN
+    const char* room_code; // private room invite code (6 chars)
+    const char* token; // JWT for direct refresh (bypasses file-based token load)
+    const char* queue_mode; // "casual" or "ranked"
 } NetplayArgs;
 #endif
 
